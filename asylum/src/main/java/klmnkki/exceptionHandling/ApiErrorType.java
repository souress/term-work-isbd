package klmnkki.exceptionHandling;

public enum ApiErrorType {
    UNKNOWN_ERROR,
    FORBIDDEN_ACCESS,
    USER_NOT_FOUND,
    PERSON_NOT_FOUND,
    USER_ALREADY_EXISTS,
    INCORRECT_LOGIN_LENGTH,
    WRONG_PASSWORD
}
